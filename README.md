# Job Queue MVP

Quickstart:
1. cp infra/.env.example .env
2. docker-compose -f infra/docker-compose.yml up -d
3. cd packages/api && npm install && npm run dev
4. cd packages/worker && npm install && npm run dev

API: http://localhost:3000
